# iSponsorBlock

Setup docs: https://github.com/dmunozv04/iSponsorBlockTV/wiki/Installation

Run the following to config/pair:

```
docker run --rm -it -v ./data:/app/data --net=host ghcr.io/dmunozv04/isponsorblocktv --setup-cli
```
